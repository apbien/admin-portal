<!doctype html>
<html>
    <head>
        <title>Admin Portal</title>
        <link rel = "stylesheet" href = "/stylesheets/style.css">
        <script src = "/routes/login.js"></script>
    </head>
    <body>

        <div class="header">
            <a class="logo" href="#default"><img src="/images/eggylogo.png"></a>
            <div class="header-right">
                <nav>
                    {{#if login}}
                    {{else if admin}}
                    <a href="#">Admin Links</a>
                    <a href="/logout">Sign Out</a>
                    {{else if financeAdmin}}
                    <a href="#">Admin Links</a>
                    <a href="#">Finance Links</a>
                    <a href="/logout">Sign Out</a>
                    {{else if salesAdmin}}
                    <a href="#">Admin Links</a>
                    <a href="#">Sales Links</a>
                    <a href="/logout">Sign Out</a>
                    {{else if hrAdmin}}
                    <a href="#">Admin Links</a>
                    <a href="#">HR Links</a>
                    <a href="/logout">Sign Out</a>
                    {{else if enggAdmin}}
                    <a href="#">Admin Links</a>
                    <a href="#">Engineering Links</a>
                    <a href="/logout">Sign Out</a>
                    {{else}}
                    <a href="/logout">Sign Out</a>
                    {{/if}}
                </nav>
            </div>
        </div>

        {{{body}}}

    </body>
</html>
